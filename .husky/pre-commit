#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# API route sanity checks (replaces legacy service guardrails)
.husky/pre-commit-api-sanity.sh || exit 1

# Run lint-staged for all staged files
# DISABLED: Takes unreasonable time with eslint --fix
# npm run lint-staged
