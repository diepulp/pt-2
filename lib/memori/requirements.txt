# Memori SDK dependencies for PT-2

# Core Memori SDK
memorisdk>=0.1.0

# OpenAI (required by Memori for embeddings)
openai>=1.0.0

# Database adapters
psycopg2-binary>=2.9.9
asyncpg>=0.29.0  # Async PostgreSQL for SessionService

# Logging
loguru>=0.7.2

# Optional: Vector embeddings (if pgvector is used)
# Note: pgvector extension must be installed in PostgreSQL
