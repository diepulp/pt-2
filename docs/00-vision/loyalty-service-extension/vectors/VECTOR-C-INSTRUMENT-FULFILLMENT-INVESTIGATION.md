# Vector C — Reward Instrument Fulfillment Investigation

**Project:** Casino Player Tracker
**Vector:** Reward Instrument Fulfillment (Pilot Print Standard)
**Status:** Investigation complete — ready for `/feature-pipeline` intake
**Date:** 2026-03-18
**Governing artifacts:**
- `LOYALTY_PILOT_SLICE_BOUNDARY.md` §2-3 (Pilot Reward Instrument Print Standard)
- `loyalty_pilot_implementation_structuring_memo.md` (Exec Spec C)
- `REWARD_FULFILLMENT_POLICY.md` (fulfillment classification model)
- `MATCHPLAY-PRINT-v0.1.md` (print infrastructure draft spec)

---

## Vector definition

This vector covers everything required to produce a printable physical artifact when a pilot reward is issued — the constrained print standard, per-family templates, rendering mechanism, print trigger, print feedback, and minimal reprint support.

**Scope:** pilot reward instrument print standard, comp slip template, coupon template, iframe print utility, print trigger per issuance flow, print success/failure feedback, minimal reprint.

**Not in scope:** admin catalog CRUD (Vector A), issuance logic or persistence (Vector B), generalized print/document platform.

---

## Current system posture

### What exists: NOTHING

The system has **zero printing infrastructure**. This is confirmed by comprehensive codebase search:

| Search target | Result |
|---------------|--------|
| `window.print()` | 0 matches |
| `@media print` | 0 matches |
| `iframe` print patterns | 0 matches |
| `lib/print/` directory | Does not exist |
| Print-related components | 0 matches |
| Print CSS files | 0 matches |
| PDF generation libraries | Not installed |
| Print history logging | No implementation |
| Template rendering for print | 0 matches |

### Related infrastructure that does exist

| Asset | Relevance | Location |
|-------|-----------|----------|
| `promo_coupon.metadata` (JSONB) | Would store `print_history[]` entries | **MISSING — column does not exist yet (see GAP-C0)** |
| `promo_coupon.validation_number` | Printed on coupon artifacts | Generated by `rpc_issue_promo_coupon` |
| Casino name/branding | Printed on all artifacts | `casinos.name` table |
| Player display name | Printed on all artifacts | `player_casino.display_name` or `players.first_name/last_name` |
| Staff identity | "Issued by" on artifacts | Derived from JWT via `set_rls_context_from_staff()` |
| Tier information | Printed on entitlement coupons | `player_loyalty.tier` |
| `IssueRewardButton` | Future print trigger host | `components/player-360/header/issue-reward-button.tsx` (disabled stub) |

---

## Fulfillment classification (from REWARD_FULFILLMENT_POLICY.md)

The system recognizes three fulfillment families. Only the first two require printing:

| Family | Mechanism | Artifact | Print Policy |
|--------|-----------|----------|--------------|--------------|
| `points_comp` | ledger debit → comp record | **comp_slip** | **Required** |
| `entitlement` | coupon issuance record | **coupon** | **Required** |
| `internal_adjustment` | admin-only record | none | **Not required** |


### Critical structural rule

> Comp slips and coupons must NOT be collapsed into one domain model.

They may share a constrained print rendering path at the fulfillment layer, but:
- Storage and issuance semantics remain distinct
- Templates are per-family (comp slip ≠ coupon)
- Domain meaning is preserved through the print path

---

## What must be built (full scope)

### 1. Print rendering mechanism

**Approach:** Hidden iframe + `window.print()` (browser print dialog)

**Implementation outline:**
1. Button click (user gesture — required for `print()` to work)
2. Issuance RPC returns fulfillment payload (from Vector B)
3. Build printable HTML string from payload + player/casino fields
4. Create hidden iframe, write HTML into iframe document
5. Wait for readiness (fonts + 2x `requestAnimationFrame`)
6. Call `iframeWindow.print()`
7. Cleanup iframe after print dialog closes (best-effort)

**Location:** `lib/print/` (new directory)

**Files needed:**
- `lib/print/iframe-print.ts` — iframe creation, write, wait, print, cleanup utility
- `lib/print/templates/comp-slip.ts` — comp slip HTML builder
- `lib/print/templates/coupon.ts` — coupon HTML builder
- `lib/print/types.ts` — fulfillment payload types (CompSlipPayload, CouponPayload)
- `lib/print/print-styles.css` — shared print CSS (minimal, system fonts, `@page` margins)

### 2. Per-family templates

#### Comp slip template (`points_comp` family)

**Required fields:**
| Field | Source |
|-------|--------|
| Casino name | `casinos.name` |
| Player name | `player_casino.display_name` |
| Comp type | `reward_catalog.kind` (meal, beverage, misc, etc.) |
| Face value | `reward_catalog.face_value` or issuance record |
| Points redeemed | `loyalty_ledger.points_delta` (absolute) |
| Post-redemption balance | `player_loyalty.current_balance` (after debit) |
| Issued by (staff) | Staff name from RLS context |
| Issued date/time | Server timestamp |
| Reference number | Ledger transaction ID or generated reference |

**Layout:** Simple, generous spacing. Casino header → player info → comp details → staff/timestamp footer.

#### Coupon template (`entitlement` family)

**Required fields:**
| Field | Source |
|-------|--------|
| Casino name | `casinos.name` |
| Player name | `player_casino.display_name` |
| Reward type | `promo_program.promo_type` (match_play, free_play) |
| Face value | `promo_coupon.face_value_amount` |
| Required match wager | `promo_coupon.required_match_wager_amount` (match play only) |
| Validation number | `promo_coupon.validation_number` (monospaced) |
| Expiry | `promo_coupon.expires_at` (if applicable) |
| Tier basis | `player_loyalty.tier` at time of issuance |
| Issued by (staff) | Staff name from RLS context |
| Issued date/time | `promo_coupon.issued_at` |

**Layout:** Simple, generous spacing. Casino header → player info → coupon details → validation number (prominent) → staff/timestamp footer.

### 3. Print trigger integration

Each issuance flow (Vector B) must include a print action:

| Family | Trigger point | Action |
|--------|--------------|--------|
| `points_comp` | After comp issuance success | Build comp slip payload → iframe print |
| `entitlement` | After coupon issuance success | Build coupon payload → iframe print |

**Implementation:** Print trigger fires in mutation hook `onSuccess` callback. The issuance response includes all fields needed for the template. No additional API call required.

### 4. Print feedback

- **Success:** Print dialog opens → operator uses native browser print → dialog closes → UI shows "Printed" confirmation
- **Failure:** If iframe creation or `print()` call fails → UI shows error toast with "Print failed — try again" + manual retry button
- **No blocking:** Print logging (GAP-C3) must not block the print dialog

### 5. Minimal reprint

- Reprint the same coupon without creating a new issuance
- Reprint comp slip from history view (re-renders from persisted data)
- Reprint appends to `print_history[]` in metadata

---

## Gap analysis

### GAP-C0: `promo_coupon` table missing `metadata` column (P0)

**Impact:** Cannot store print history, channel tracking, or tier-at-issuance metadata.

**Finding:** Codebase investigation confirmed `promo_coupon` table (migration `20260106235611`, lines 91-115) has 20 columns but **no `metadata jsonb` column**. The `MATCHPLAY-PRINT-v0.1.md` spec assumes `promo_coupon.metadata.print_history[]` exists for print logging, and `rpc_issue_current_match_play` needs to write `channel`, `tier`, `policy_version`, `computed_at` at issuance time.

**Fix:** New migration adding `metadata jsonb NOT NULL DEFAULT '{}'::jsonb` to `promo_coupon`. Then `npm run db:types-local` to regenerate types. Update promo service CRUD to handle metadata reads/writes.

**Blocking:** Yes — print history logging, channel tracking, and tier-at-issuance recording all depend on this column existing.

### GAP-C1: No iframe print utility (P0)

**Impact:** Nothing can print. This is the foundation.

**Build scope:** ~80-120 lines. Hidden iframe creation, HTML injection, readiness wait, `window.print()` call, cleanup.

**Design constraints:**
- Must work with browser print dialog (no silent/background printing)
- System fonts only (no web font loading)
- Explicit `@page` margins
- Tolerant of 95-105% scaling
- No navigation (iframe stays hidden)

### GAP-C2: No per-family templates (P0)

**Impact:** Even with print utility, no content to render.

**Build scope:** ~100-150 lines per template (comp slip + coupon = ~200-300 lines total).

**Design constraints:**
- Large amounts, generous spacing
- No complex flex/grid edge cases
- No fragile absolute positioning
- Monospaced validation numbers
- Casino header with property name

### GAP-C3: No print history logging (P2)

**Impact:** No audit trail for prints. Acceptable for pilot with manual workaround.

**Build scope:** Best-effort append to `promo_coupon.metadata.print_history[]`:
```json
{
  "printed_at": "2026-03-18T10:25:00Z",
  "printed_by_staff_id": "uuid",
  "channel": "rating_slip_print_button",
  "device_hint": "Chrome/Windows"
}
```

**Important:** Print logging must NOT block printing. Fire-and-forget after print dialog opens.

### GAP-C4: No comp slip reference number system (P1)

**Impact:** Comp slips need a traceable reference number. Coupons use `validation_number` from `promo_coupon`. Comp issuance via `loyalty_ledger` has no equivalent.

**Options:**
- Use `loyalty_ledger.id` (UUID — not human-readable)
- Generate a short reference code at issuance time
- Use a composite key (date + sequence)

**Decision required:** Reference number format for comp slips.

### GAP-C5: QR/barcode decision (P3)

**Impact:** Optional scan capability for validation numbers on coupons.

**Recommendation from posture audit:** Defer unless floor benefits from scan speed. Not required for pilot.

---

## Print standard specification (from governing boundary doc)

> Build the smallest print surface that makes every pilot instrument type usable on the floor. Stop there.

### What the print standard IS:
1. One rendering path (hidden iframe + `window.print()`)
2. Per-instrument templates (comp slip + coupon)
3. Common fields across all prints (casino, player, date, staff, reference)
4. Instrument-specific fields per template
5. Print trigger per issuance flow
6. Print success/failure feedback
7. Minimal reprint (manual workaround acceptable)

### What the print standard is NOT:
- Not a document management system
- Not a template registry with versioning
- Not a printer fleet abstraction
- Not a generic rendering engine
- Not designed for extensibility beyond the pilot instrument set

---

## Contract surface this vector consumes

From Vector B (issuance), this vector needs:

### Comp slip payload contract
```typescript
interface CompSlipPayload {
  casinoName: string;
  playerDisplayName: string;
  compType: string;           // reward_catalog.kind (meal, beverage, etc.)
  faceValue: number;          // dollar amount
  pointsRedeemed: number;     // absolute value of points_delta
  postBalance: number;        // player_loyalty.current_balance after debit
  issuedByStaffName: string;  // from RLS context
  issuedAt: string;           // ISO timestamp
  referenceNumber: string;    // traceable reference (TBD format)
}
```

### Coupon payload contract
```typescript
interface CouponPayload {
  casinoName: string;
  playerDisplayName: string;
  rewardType: string;         // match_play, free_play
  faceValue: number;          // promo_coupon.face_value_amount
  requiredMatchWager?: number; // match play only
  validationNumber: string;   // promo_coupon.validation_number
  expiresAt?: string;         // ISO timestamp if applicable
  tierBasis: string;          // player tier at issuance
  issuedByStaffName: string;
  issuedAt: string;
}
```

---

## Acceptance criteria (from governing boundary doc)

- [ ] Each pilot instrument type (comp slip, coupon) has a printable template the floor accepts
- [ ] Print output contains sufficient information for the floor to honor the reward
- [ ] Print mechanism works on standard browser print (casino floor hardware)
- [ ] Operator receives clear success/failure indication after print attempt
- [ ] A floor user can complete the print process without developer intervention
- [ ] Reprint produces the same artifact without creating a new issuance
- [ ] Comp slips and coupons remain structurally distinct (different templates, different payloads)

---

## Design decisions required before build

| # | Decision | Impact | Recommendation |
|---|----------|--------|----------------|
| D1 | Comp slip reference number format | Affects traceability and template content | Short alphanumeric code (e.g., `CS-20260318-0042`) |
| D2 | QR/barcode on coupon validation number | Affects template complexity | Defer for pilot |
| D3 | Print trigger location (modal vs inline) | Affects UX integration with Vector B | Inline in issuance success callback |
| D4 | Reprint entry point (history list vs detail view) | Affects UI surface for reprint | Both: small "reprint" icon in history list + button in detail |
| D5 | Print CSS approach (inline vs separate file) | Affects template maintenance | Inline in template HTML (self-contained iframe document) |

---

## Recommended file structure

```
lib/print/
├── iframe-print.ts           # Core utility: create iframe, inject HTML, print, cleanup
├── types.ts                  # CompSlipPayload, CouponPayload, PrintResult types
├── templates/
│   ├── comp-slip.ts          # buildCompSlipHtml(payload: CompSlipPayload): string
│   ├── coupon.ts             # buildCouponHtml(payload: CouponPayload): string
│   └── shared-styles.ts      # Common CSS string (system fonts, @page, spacing)
└── hooks/
    ├── use-print-comp-slip.ts    # Hook: builds payload + calls iframe print
    └── use-print-coupon.ts       # Hook: builds payload + calls iframe print
```

---

## Dependencies

| Dependency | Type | Status |
|------------|------|--------|
| Vector B: issuance contract freeze (comp slip + coupon payloads) | **Blocks** template building | Pending |
| Vector B: issuance mutation hooks `onSuccess` | **Blocks** print trigger wiring | Pending |
| Casino name availability in client context | Required | Exists (casino settings in Zustand store) |
| Player display name in issuance response | Required | Exists in player DTOs |
| Staff name in client context | Required | Exists (auth context) |
| `promo_coupon.validation_number` | Required | Generated by existing RPC |

---

## Risk assessment

| Risk | Probability | Mitigation |
|------|-------------|-----------|
| Browser print dialog unreliable on floor hardware | Medium | Test on actual hardware early. Fallback: manual workaround (screenshot + print). |
| Print scaling varies across browsers | Medium | System fonts, `@page` margins, 95-105% tolerance design |
| Iframe `print()` blocked by popup settings | Low | Requires user gesture (button click) — should be allowed |
| Print audit logging fails silently | Low | Non-blocking by design. Manual log acceptable for pilot. |

---

## Feature pipeline handoff

This artifact is ready for `/feature-start "Reward Instrument Fulfillment Slice"`.

**Key input for Phase 0 (SRM-First Ownership):**
- Owner: loyalty bounded context (fulfillment sub-domain)
- New module: `lib/print/` (no existing tables — consumes issuance records)
- Cross-context reads: casino (name), player (display name), staff (name)

**Key input for Phase 2 (RFC/Design Brief):**
- Zero existing infrastructure — full greenfield build
- Two templates needed (comp slip + coupon)
- Print utility is framework-level code (~80-120 lines)
- D1-D5 decisions must be made

**Key input for Phase 4 (ADR):**
- ADR: Pilot Reward Instrument Fulfillment Standard
  - `points_comp` and `entitlement` remain structurally distinct
  - Comp slips and coupons are not collapsed into one domain model
  - Constrained shared print path allowed only at rendering layer
  - Internal/admin adjustments remain non-print
  - No generalized print/document platform authorized

**Dependency on other vectors:**
- **Blocks on Vector B:** issuance contract must be frozen before templates can be built
- Can develop iframe print utility in parallel with Vector B (no issuance dependency)
- Can develop template skeletons in parallel with Vector B (with placeholder data)
- Full integration requires Vector B mutation hooks to fire print trigger
